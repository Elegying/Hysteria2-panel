# 变更日志

## [0.19.1] - 2026-08-16

### 修复

- 修复手机端用户管理标题在纵向布局中继承桌面 `flex-basis`，导致“添加用户”和“编辑用户”按钮前出现大块空白的问题；桌面布局不变。

### 测试

- 增加手机断点标题占位回归测试，并在 320px、390px 的真实 Edge 渲染中验证按钮紧随说明文字、页面无横向溢出或控制台错误。

## [0.19.0] - 2026-08-16

### 新增

- 用户管理的每个节点新增“二维码”操作；点击后才通过现有登录、CSRF 与用户版本保护的分享接口生成配置二维码，可复制原 URI 或保存为 PNG。
- 二维码由随版本固定并校验 SHA-256 的 MIT 纯 Python 编码器在服务器本地生成，不调用第三方二维码服务；普通分享响应与现有节点配置保持不变。

### 界面

- 节点弹窗增加可访问的 Canvas 二维码、保存反馈和凭据提醒；关闭后立即清空 URI 与像素数据。
- 手机端用户操作区调整为两行三列，二维码与弹窗宽高随视口收缩，桌面端保持紧凑的单行操作。

### 测试

- 新增二维码定位图形、矩阵边界、按需响应、初始 HTML 无凭据、移动端布局及安装器依赖固定回归测试。
- 对上游编码器的私有位缓冲基类应用无行为变化的 Python 3.8 兼容写法，并由双版本 CI 验证导入与完整测试。

## [0.18.1] - 2026-08-15

### 安全与可靠性

- 内部 Hysteria HTTP 认证监听地址现在强制为 IPv4 回环 `127.0.0.1`，防止误配置后把无独立传输认证的内部端点暴露到外部网络。
- 同一来源地址的管理员密码校验改为有界分片串行，关闭并发请求同时越过登录失败次数检查的竞态；不同来源仍可并行登录。
- 在线更新检查与排队共享互斥保护，确保排队和审计使用同一个已核验版本；删除用户时同步清理其短期在线与连接预留状态。
- root 恢复前置收口和恢复后健康确认会接受由受保护事务标记记录的自定义数据库与 TLS 路径，继续保留 root 所有权、文件模式、普通文件及无符号链接校验。

### 修复

- 备份 ZIP 上传上限与允许的最大解压内容保持一致，避免合法的大型数据库备份由面板自身生成后却无法恢复。
- 非 BLOB 用户令牌种子现在作为可诊断的备份验证错误拒绝，不再泄漏底层类型异常。
- 恢复文件替换会在原子改名之前通过已打开的文件描述符写入所有权和模式并统一 `fsync`，补齐断电恢复时的元数据持久性。

### 测试

- 新增登录并发、更新竞态、用户瞬时状态清理、自定义恢复路径、最大备份、损坏令牌种子、恢复文件元数据持久性和内部认证绑定回归测试。

## [0.18.0] - 2026-08-13

### 新增

- 用户列表增加用户名搜索、启用/禁用、在线/离线及 UDP `443` 已开放/未开放组合筛选；筛选条件同步到当前 URL，点击在线设备或总流量排序时会继续保留，并提供一键清除和无匹配结果提示。

## [0.17.0] - 2026-08-13

### 新增

- 用户管理表格的“在线设备”表头支持单击排序：首次点击按在线设备数降序，再次点击切换为升序，并以箭头及无障碍排序状态显示当前方向；既有总流量排序与默认用户顺序保持不变。

## [0.16.1] - 2026-08-13

### 修复

- 修正维护切换把 Hysteria `/kick` 误当成立即断线的问题：完成有界踢线窗口后允许仍显示在 `/online` 的空闲会话由服务停止统一关闭，避免有真实在线用户时升级被错误回滚；统计端点查询或踢线失败仍会安全停止。

## [0.16.0] - 2026-08-13

### 新增

- 一键部署会先只读识别主机防火墙：UFW 已启用时自动开放节点 TCP/UDP、面板 TCP 和账号专属 TCP/UDP `443`；firewalld 已启用时对所有活动 zone 同时写入即时与永久规则。
- 未启用主机防火墙时保持规则集不变；云平台安全组仍由管理员独立放行。
- 自动添加的面板端口规则允许所有来源；升级前若主机仅靠 UFW 来源限制保护面板，需先确认云安全组已把该端口收窄到固定管理 IP，避免扩大公网暴露范围。

### 安全与可靠性

- 防火墙预检覆盖 nftables、IPv4 iptables 和 IPv6 ip6tables；自定义入站策略、双管理器或检查失败都会在应用文件、服务和防火墙规则写入前安全停止，不猜测或覆盖现有规则。
- UFW/firewalld 只补缺失规则；任一写入失败会精确撤销本次已添加项，防火墙成功后才解除应用升级回滚保护。
- 受管安装升级前验证配置所有权、数据库、HMAC、TLS 文件、公私钥匹配和证书指纹，身份残缺时停止，避免静默轮换已发放链接。
- 配置端口继续兼容既有 `1–65535` 范围；升级保留特权端口时仅给对应绑定服务授予 `CAP_NET_BIND_SERVICE`。主 Hysteria 端口不是 `443` 时，固定 `443` 仍保留给独立账号入口，避免面板或内部端口抢占。
- 升级前会暂停面板写入、在旧 Hysteria 统计端点结算流量、截断 SQLite WAL 并创建一致快照；失败回滚会优先保留通过校验的最新数据库，仅在损坏时清除 WAL/SHM 后恢复快照，避免覆盖升级窗口内已结算流量。
- UFW 最终复查全部目标 allow 与 deny；firewalld 对全部目标 zone 拒绝 rich rules，并在写入后复查 zone 和全部即时/永久端口。任何漂移都会撤销本次新增规则并停止。
- Hysteria 已清零的流量在 SQLite 临时写失败时写入受限的本地待处理日志并使用批次幂等补写；日志写入故障时仍保留进程内重试，降低额度少计风险。
- 升级最终切换会先停止认证入口、踢下并等待现有 Hysteria 客户端离线，再执行最后一次持久流量结算；该临界区延迟常规终止信号，避免正常停止打断已经清零但尚未落盘的统计。
- 恢复上传以只读共享锁准入，root 恢复与安装/更新使用同一文件的排他锁；恢复前先清退连接、结算流量并截断 WAL。版本化持久事务由启动前的文件身份收口和启动后的连续 HTTP/统计/TCP 健康验收两阶段组成，可在信号或重启后幂等继续。
- 在线更新只由 systemd 的 25 分钟启动时限和 15 分钟停止/回滚时限管理，不再由 Python 在中途强杀安装器；HTTPS TLS 握手移入受限工作线程，单个慢握手不再阻塞全部管理请求。

### 测试

- 新增可执行的 UFW、firewalld 多 zone、重复部署、IPv6、自定义跳转链、双管理器、检查故障、部分写入回滚和真实 systemd 依赖停止语义测试。

## [0.15.2] - 2026-08-12

### 修复

- TCP `443` 现在与 TCP `19999` 使用同一个兼容探测程序，支持客户端对 `443` 执行 TCP 延迟和连通性测试；探测连接建立后立即关闭，不承载代理数据。

### 兼容与可靠性

- TCP `443` 探测服务跟随 UDP `443` Hysteria 子服务启停，并以动态用户运行；仅授予绑定特权端口所需的 `CAP_NET_BIND_SERVICE`。
- 新服务纳入端口占用检查、升级备份、失败回滚、恢复任务冲突处理和安装后监听检查。TCP 探测结果不改变账号级 UDP `443` 授权。

## [0.15.1] - 2026-08-12

### 修复

- 为非 root 的 UDP `443` Hysteria 子服务仅授予 `CAP_NET_BIND_SERVICE`，修复首次生产部署时因无权绑定特权端口而触发自动回滚的问题。

## [0.15.0] - 2026-08-12

### 新增

- 编辑用户增加“允许该账号使用 UDP 443”选项；默认关闭，开启后客户端只需把原节点端口改为 `443`，原 `19999` 入口仍可继续使用，token、证书指纹和原分享 URI 不变。
- 安装器增加独立的 Hysteria UDP `443` 进程、专用认证路径和本机统计端口，并将两个入口的在线实例与流量统一计入原有限额。

### 兼容与可靠性

- 旧数据库和旧备份中的账号迁移后默认不开放 UDP `443`；备份恢复继续保留全部已发放账号身份。
- UDP `443` 服务纳入 systemd 启停、升级前备份、失败回滚和安装后监听检查；安装器仍不自动修改云安全组或主机防火墙。

## [0.14.2] - 2026-08-12

### 修复

- 默认 `web` 出站策略在拒绝环回、私网、链路本地、CGNAT 和 IPv6 ULA 后，允许所有公网目标的 SSH TCP `22`，修复连接 Hysteria 节点后无法管理公网服务器的问题。

### 安全边界

- 公网 SSH 放行不会改变用户链接、认证密钥、HMAC、TLS 证书或证书指纹，但代理账号可能被用于 SSH 扫描或爆破；继续依赖用户限额、审计、禁用和服务商滥用处置。

## [0.14.1] - 2026-08-12

### 修复

- 修复在线更新单元因 systemd 地址族沙箱不允许 `AF_NETLINK`，导致安装器在读取网卡公网地址时于第 315 行退出的问题。
- 公网地址自动探测改为可失败步骤；已有受管安装会继续使用持久配置中的域名或 IP，不再让非关键探测阻断升级。
- 更新单元加入受限的 `AF_NETLINK` 地址族，后续升级可以正常读取网卡信息，同时继续禁止无关地址族。

## [0.14.0] - 2026-08-12

### 修复

- 在线更新改为异步提交并持续显示排队、运行、成功或失败；跨面板重启保存目标状态，只有更新任务结束成功且实际版本达到目标版本才确认成功。
- 更新任务启动失败、退出码异常或结束后版本未变化时给出明确错误，不再出现点击“立即更新”后没有结果。

### 用户与运维

- 用户管理增加限额编辑弹窗，可修改单个用户的客户端实例数和总流量额度；保留 token、累计流量和已发放节点 URI。
- 在线客户端实例数大于配置上限时，用户名标红并显示非颜色文字告警。
- 系统资源模块增加带二次确认的整机重启按钮，后端只允许固定 `systemctl --no-block reboot` 命令并记录审计。

### 安全与说明

- 明确设备限制依据 Hysteria `/online` 客户端实例统计，不伪造物理设备指纹；标准认证协议没有稳定硬件 ID。
- 更新目标再次执行严格语义版本校验，状态文件原子写入且限制读取大小；JSON 调用失败时返回可诊断错误。

## [0.13.0] - 2026-08-12

### 可靠性

- 恢复写入完成后从磁盘再次逐列验证全部代理用户数据，并校验 HMAC、证书/私钥、证书指纹和有效期；任何不一致都回滚到恢复前快照。
- 恢复失败的待处理 ZIP 会以 `0600` 权限隔离保存，不再永久占用恢复队列。
- 面板、内部认证和流量采集任一工作线程异常退出时，主进程会失败退出并由 systemd 自动重启，避免进程仍显示运行但部分能力已经失联。
- 现有安装升级失败或健康检查不通过时，自动恢复旧程序、配置、节点 TLS 身份、用户数据库、服务单元、sudoers 和网络参数。

### 性能与界面

- 对支持 gzip 的浏览器压缩大型 HTML；356 用户基准响应从约 749 KB 降到约 22.7 KB，同时保持 `Cache-Control: no-store`。
- 用户搜索按动画帧合并连续输入；桌面表头粘附、数字等宽，补充悬停反馈和减少动态效果偏好；手机端继续保持单用户一行且无横向滚动。
- 迁移弹窗明确要求先在新服务器恢复并验证，再切换 DNS，避免流量提前落到尚未恢复用户身份的主机。

### 测试与维护

- 新增跨服务器多用户旧 token/URI/TLS 身份连续性、恢复后损坏回滚、失败队列恢复、工作线程失联和 gzip 协商回归测试。
- 安装下载增加固定总超时，升级前备份目录增加随机后缀，避免同一秒重复任务复用目录。

## [0.12.2] - 2026-08-12

### 修复

- 默认 `web` ACL 在拒绝私网目标后统一放行公网面板 TCP 端口，使旧节点能够访问以后新增服务器的面板，不再要求每增加一台服务器就更新所有旧节点的精确 IP 列表。
- 移除 `PANEL_ACCESS_IPS` 部署参数；现有用户、分享密钥、HMAC 与 TLS 身份不受影响。

### 安全边界

- 用户也能访问互联网上其他使用面板 TCP 端口的服务，但环回与私网目标仍先被拒绝，面板认证、会话、CSRF 和登录锁定保持不变。

## [0.12.1] - 2026-08-12

### 修复

- `web` 出站策略支持仅对指定面板 IP 放行当前管理端口，修复用户开启同一 Hysteria 节点后无法访问该节点面板的问题。
- 新增并持久化 `PANEL_ACCESS_IPS`；全新安装默认使用服务器检测 IP，在线更新和重复部署沿用现有列表，不修改用户链接、认证密钥或 TLS 身份。

### 安全

- 面板例外只接受最多 16 个经过标准库校验和规范化的精确 IPv4/IPv6 地址，不允许 `direct(all, tcp/面板端口)` 扩大到任意外部主机。

## [0.12.0] - 2026-08-11

### 新增

- 版本检查发现新正式版本时显示“立即更新”，通过固定的一次性 systemd 服务执行在线升级。
- 新增安装器非交互更新模式，强制沿用现有节点名、域名、端口、面板协议、出站策略和内部端口。

### 安全

- 在线更新接口继续要求管理会话和 CSRF，不接收浏览器传入的版本、地址或命令。
- root 更新器只访问固定 GitHub 仓库，只接受严格语义版本，并校验远程安装器大小、编码、解释器头、内嵌版本和 shell 语法。
- 在线升级保留数据库、管理员、HMAC/统计密钥及 TLS 证书私钥；全新或非受管安装拒绝进入内部更新模式。

## [0.11.2] - 2026-08-11

### 新增

- 面板登录页和控制台增加与现有深蓝、青绿色视觉一致的 H2 网站图标。
- 图标由面板本地提供，不依赖第三方静态资源；严格内容安全策略仅新增同源图片许可。

## [0.11.1] - 2026-08-11

### 修复

- 修复同一账号有多个客户端实例时，流量超额后只断开第一个产生流量的实例、其余实例仍可继续使用的问题。
- 后台同步会对仍在线的超额、禁用或已删除账号持续批量请求清退，直到在线统计归零；认证后端继续拒绝其重连。

### 优化

- 合并同一轮需要清退的用户名到一次 Hysteria `/kick` 请求，避免按用户逐个调用统计接口。
- 补充 Ubuntu 24.04 全新安装、多设备限制、真实网页流量和默认出站 ACL 的实机验收说明。

## [0.11.0] - 2026-08-11

### 安全

- 默认启用网页/视频出站 ACL，拒绝私网访问和非网页端口，阻断常规 BT/PT、DHT、uTP、邮件滥发与扫描；保留显式 `EGRESS_POLICY=full` 兼容模式。
- 文档明确端口 ACL 不是 DPI，无法保证识别伪装在 80/443 或外部中继中的加密 P2P。

### 修复与说明

- 把“设备数”统一修正为 Hysteria 客户端实例数：默认前三个实例继续在线，第 4 个认证被拒绝且不踢旧实例。
- 出站策略在重复部署时持久保留，不改变用户认证密钥、分享链接、TLS 证书或证书指纹。

## [0.10.1] - 2026-08-11

### 修复

- 明文 HTTP 域名被链路设备重置时，不再为预期的客户端断连输出完整服务异常栈。
- 节点使用域名且面板为 HTTP 时，一键部署结果同时显示本机检测 IP 备用面板地址，避免域名 Host 被干预后无法登录。

## [0.10.0] - 2026-08-11

### 安全

- 管理员登录按来源 IP 防破解：15 分钟内第 5 次错误立即锁定 15 分钟并返回 `429` 与 `Retry-After`；未知账号执行等价密码校验，记录数量有固定上限。
- 管理员账号或密码变更会确保只保留一个管理员并撤销全部旧会话；升级默认不再重置管理员。
- 拒绝带异常高成本参数的密码哈希，限制本地 Hysteria 统计响应为 8 MiB，并强制统计接口只能使用环回 HTTP。
- 下载的面板程序和 TCP 探测程序新增固定 SHA-256 校验。

### 优化

- 全新安装默认使用 HTTP 面板；Hysteria 数据通道的 TLS 与证书固定保持不变。
- Hysteria 明确使用 `bbr` 的 `standard` profile 并忽略客户端带宽上报；UDP 缓冲提高到至少 16 MiB，服务设置 `Nice=-5`。
- 内核支持时安全启用 `fq`/TCP BBR；不支持时恢复原值并继续部署，不写入无依据的额外 sysctl。

### 修复与兼容

- 安装器支持 `apt`、`dnf`、`yum`、Linux amd64/arm64、systemd 和 Python 3.8+，并补全运行命令预检。
- 升级自动沿用节点名、域名、外部/内部端口、面板协议、管理员、HMAC/统计密钥和 TLS 身份，避免旧分享节点意外失效。
- CI 增加 Python 3.8/3.12、Ruff、Bandit、ShellCheck、语法和补丁格式检查；GitHub 文档统一为中文并明确 HTTP、BBR、备份和真机验收边界。

## [0.9.1] - 2026-08-11

### 变更

- BBR 状态与当前版本改为两个独立卡片，与服务摘要布局一致。
- 用户搜索框移入管理区标题栏，去掉可见文字标签但保留无障碍名称。

## [0.9.0] - 2026-08-11

### 新增

- 支持在仪表盘按用户名即时搜索全部用户。

### 变更

- 完整显示用户列表，不再分页。
- 添加用户改为响应式弹窗，入口放在重置全部流量旁边。

## [0.8.0] - 2026-08-11

### 新增

- 在当前版本旁显示 Hysteria QUIC BBR profile，以及实时 Linux TCP 拥塞控制和队列规则。

### 变更

- 用户默认按创建时间倒序排列，同时保留总流量排序。
- 一键部署明确固定 Hysteria 非 Brutal BBR 的 `standard` profile。

## [0.7.0] - 2026-08-10

### 新增

- 用户列表支持按总流量升序或降序排列。
- 新建用户后在当前页面弹出节点信息和复制按钮。

### 变更

- 分享按钮直接复制 Hysteria 2 URI，不再跳转页面。
- 手机端每行显示一个紧凑用户，五个操作均可见且无需横向滚动。
- 高流量用户的流量放在用户名后，桌面三个运维卡片保持等高。
- 删除用户名下方重复的并发连接限制说明。

## [0.6.1] - 2026-08-10

### 修复

- 手机登录按钮与密码框完全分离，不再重叠。
- 修复服务摘要与版本卡片之间的异常间距。

### 变更

- 服务控制、系统资源和高流量用户在桌面端排为一行。
- 缩小流量统计、端口、版本、更新和资源卡片占用，手机摘要仍保持双栏。

## [0.6.0] - 2026-08-10

### 新增

- 增加原生、支持键盘操作的数据迁移弹窗。

### 变更

- 收紧桌面间距，重组手机端导航、指标和服务控制。
- 宽用户表在手机端转换为带标签的触控卡片，桌面表格保持不变。

## [0.5.1] - 2026-08-10

### 修复

- 每次备份恢复后都会重新启动 TCP `19999` 兼容探测，包括恢复校验失败的情况。

## [0.5.0] - 2026-08-10

### 新增

- 一键下载/上传 ZIP，迁移代理用户、持久流量、HMAC 身份、TLS 证书/私钥、证书指纹和清单信息。
- 增加 ZIP、SQLite、token、证书/私钥、指纹和端点严格校验，以及带自动备份和回滚的隔离 root 一次性恢复服务。

### 变更

- 恢复时保留目标面板管理员和部署专用参数并使旧会话失效；公网地址或端口变化、证书续签/轮换时仍需重新分享。

## [0.4.0] - 2026-08-10

### 新增

- 在 Hysteria UDP 相同数字端口提供加固的 TCP 连通性探测，兼容 TCP-only 客户端测试，不暴露面板或认证数据，也不替代协议级健康检查。

## [0.3.2] - 2026-08-10

### 修复

- 删除面板 unit 中仍会隐式阻止 setuid 的沙箱指令，使精确 sudoers 服务控制可用；Hysteria 服务继续保留完整沙箱。

## [0.3.1] - 2026-08-10

### 修复

- 从面板 unit 删除与 sudo 不兼容的 `PrivateDevices=true`；Hysteria 服务继续保留该限制。

## [0.3.0] - 2026-08-10

### 新增

- 每用户并发连接和总流量限制，默认 3 个连接、250 GiB。
- 持久上传/下载统计、配额进度、单用户和全部流量重置。
- 服务端密钥派生的可恢复分享凭据与一键复制 URI。
- 深色运维仪表盘：系统资源、高流量前五、版本检查和 Hysteria 服务控制。

### 变更

- Hysteria 认证会拒绝达到连接数或流量上限的用户，同时保持官方 HTTP auth 响应契约。
- 安装器仅授权面板启动、停止和重启项目专用 Hysteria 服务。

## [0.2.1] - 2026-08-10

### 修复

- 修复浏览器残留旧 HTTPS-only Cookie 时的 HTTP 登录循环。
- HTTPS 流量误发到 HTTP 端口时，请求日志不再抛出异常。

## [0.2.0] - 2026-08-10

### 新增

- 部署时询问统一节点名称并写入所有 URI fragment。
- 增加可选 HTTP 面板，以及随协议变化的 Cookie、HSTS 和健康检查。
- 增加服务状态、用户数、不活跃用户、在线设备、总上传和总下载卡片。
- 持久化 quic-go UDP 收发缓冲优化并提高 Hysteria 文件描述符上限。

### 变更

- 重复运行安装器时，会应用用户提供的新管理员密码，而不再静默忽略。

## [0.1.2] - 2026-08-10

### 修复

- 接受 Hysteria `POST /kick` 的成功空响应，不再误报 JSON 解码失败。

## [0.1.1] - 2026-08-10

### 修复

- systemd 重启后最多等待 30 秒，确认面板和本地认证端点就绪。

## [0.1.0] - 2026-08-10

### 新增

- 对 Linux amd64/arm64 提供 Hysteria 2 `v2.12.1` 固定版本和校验和的一键安装。
- 使用 Hysteria 本机 HTTP 回调实现动态多用户认证。
- 提供带 CSRF、登录限速和加固 Cookie 的管理面板。
- 提供一次性用户凭据、固定证书指纹的连接 URI、流量统计和连接踢除。
- 提供隔离系统身份、有界并发、加固 systemd、升级备份和健康检查。
