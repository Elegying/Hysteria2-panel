# 安全政策

## 支持版本

项目只为 GitHub 上的**最新正式版本**提供安全修复。旧版本可能仍能运行，但不会单独维护安全补丁分支。报告问题前请核对 [Latest Release](https://github.com/Elegying/Hysteria2-panel/releases/latest)。

## 私密报告漏洞

请使用 GitHub 的 [Private Vulnerability Reporting](https://github.com/Elegying/Hysteria2-panel/security/advisories/new) 提交，不要创建公开 Issue、Discussion 或 PR，也不要附上真实生产凭据。

报告中建议包含：

- 受影响版本和组件；
- 前置条件与攻击者需要的权限；
- 最小可复现步骤或测试代码；
- 可能影响的数据、身份、流量或服务器权限；
- 你已经尝试的缓解措施；
- 是否已经在其他地方公开。

请对域名、IP、用户名、token、私钥、备份和日志中的敏感字段做脱敏。若需要交换完整证据，请先在私密报告中说明，不要自行上传到公开文件服务。

## 响应方式

维护者会尽力：

1. 确认报告已收到；
2. 复现并评估影响范围；
3. 与报告者协调修复和披露时间；
4. 在受保护分支和发布门禁中验证修复；
5. 发布包含修复的正式版本，并在适当时提供安全公告。

这是社区维护项目，不承诺固定 SLA。若漏洞正在被利用或会直接暴露大量用户凭据，请在标题中明确标注紧急程度和已知利用情况。

## 安全范围

欢迎报告：

- 未授权管理操作、会话或 CSRF 绕过；
- 用户认证、设备或流量限制绕过；
- 节点签名、重放保护或来源绑定缺陷；
- 安装器、更新、恢复、sudoers 或 systemd 的权限提升；
- 恶意备份、路径、符号链接或竞态导致的文件覆盖；
- Release、Sigstore、SBOM 或固定来源更新的供应链绕过；
- 敏感信息进入日志、页面、指标、备份或公开仓库。

通常不属于漏洞：

- Hysteria TUN 不代理 ICMP；
- TCP 探测可连接但 UDP/QUIC 不可用；
- `FULL` 模式本身允许公网 BT/PT、SMTP 或非标准端口；
- 管理员明确选择 HTTP 后产生的明文传输风险；
- 已取得服务器 root 权限后读取本机 root-only 文件；
- 纯粹的版本过旧、缺少最佳实践或没有可利用影响的扫描结果。

如果不确定是否属于安全问题，请优先使用私密报告。

## 管理员安全建议

- 使用 HTTPS 面板，并在云安全组中限制面板端口来源；
- 使用独立强密码，不在命令历史或聊天中传播；
- 把完整备份视为节点凭据并加密或离线保存；
- 只从正式 Release 或 README 说明的受保护入口安装；
- 定期检查 `/readyz`、节点控制指标、备份状态和证书到期提醒；
- 对 `FULL` 出站制定流量上限和滥用处置流程。
