# 界面设计验收记录

> 本文保存 v0.35.0–v0.36.2 的界面验收证据。当前用户操作和视觉行为以最新代码、[使用指南](docs/USER_GUIDE.md)及浏览器回归为准。

## 当前证据

- 实现后的桌面仪表盘：`docs/screenshots/dashboard-1380x702.png`，CSS 视口为 1380 × 702。
- 实现后的用户管理：`docs/screenshots/user-table-1380x702.png`，使用相同视口。
- 手机端：`docs/screenshots/dashboard-mobile-375x812.png`，视口为 375 × 812。
- 完整步骤与审查结论：`docs/reviews/2026-08-30-v0.35.0-stabilization-audit.md`。

本轮截图全部来自 2026-08-30 当前工作树，不再把 2026-08-10 的旧页面作为当前证据。实现保留深海军蓝运维仪表盘层级、紧凑状态标记、四个汇总卡片、运维模块布局、带边框的资源卡、高对比服务按钮和高密度用户管理区。

## 已完成的功能与响应式检查

- 管理员登录后可进入仪表盘。
- 检查更新可以显示新版本链接，不产生脚本错误。
- 创建限制为 4 个连接、123 GiB 的用户后，界面显示相同限额和 0.0% 进度。
- 分享按钮生成 Hysteria 2 URI；HTTP 页面使用兼容复制方案后，按钮会显示“已复制”。
- 重启 Hysteria 后服务恢复为运行状态。
- 375 × 812 视口没有横向溢出，顶部、指标和控制区会重排为手机布局；每行只显示一个用户。
- 添加用户弹窗支持 Esc 关闭，打开后聚焦第一个字段，关闭后焦点回到触发按钮。
- CI 使用真实 Chrome 分别渲染 1380 × 900 和 375 × 812 的认证后页面，并验证 PNG 尺寸与非空内容。

## 证据边界

截图和浏览器结果不替代真实服务器、手机、200% 缩放或屏幕阅读器测试。当前自动化覆盖桌面/手机渲染、登录、弹窗、搜索、标签、Esc 与焦点回归；最终触控、辅助技术和网络体验仍由发布候选实机验收。

## 结果

当前工作树通过桌面与手机浏览器验收；发布前仍按 `docs/STABILIZATION.md` 完成辅助技术和实机抽查。

## 2026-08-31 手机端服务控制修复

### 对比目标与证据

- 对照来源：用户提供的移动端问题截图（不把本机临时路径写入公开仓库）。
- 实现截图：`docs/screenshots/dashboard-mobile-service-controls-390x844.jpg`。
- 原图像素：1080 × 2347，包含手机浏览器和系统状态栏；实现截图像素与 CSS 视口均为 390 × 844，deviceScaleFactor 1，比较时只判断应用拥有的服务控制区域，不把浏览器外壳作为设计差异。
- 状态：管理员已登录、Hysteria 运行中、当前版本 v0.36.1、尚未检查远程版本。
- 浏览器检查：真实内置浏览器渲染；登录、服务控制区和版本检查控件均可访问；页面控制台未出现脚本错误。

### 比较历史

1. 原始证据中的 P1：服务控制区“刷新”链接没有像同组按钮一样水平和垂直居中，视觉上表现为文字偏左。修复后链接使用与按钮一致的 flex 居中规则，在 390px 视口中尺寸为 160 × 44px。
2. 原始证据中的 P1：版本卡片继续使用桌面横排按钮组，导致“检查更新”按钮被压窄，文字溢出按钮背景并侵入版本信息。修复后手机端版本信息与操作区改为单列，按钮独占卡片内容宽度；390px 视口中按钮为 129 × 44px，`scrollWidth === clientWidth`。
3. 修复后再次对照原图与实现截图：字体、颜色、圆角、卡片边框和双列 BBR/版本层级保持原设计；没有新增图片或图标资产；文案未改变。

### 响应式与交互复核

- 检查宽度：320、340、375、390、430、640px。
- 所有宽度下文档 `scrollWidth` 等于视口宽度，没有横向溢出。
- “刷新”和“检查更新”在所有宽度下均保持 44px 触控高度，文字没有裁切、换行或越过背景。
- 原有按钮语义、链接目标、更新表单、CSRF 字段和服务操作行为保持不变。
- 真实 Chrome 冒烟测试通过 1380 × 900 与 375 × 812 两个视口。

### 五个保真面

- 字体与排版：沿用系统字体栈、字号和字重；修复只调整对齐与响应式排列。
- 间距与布局：服务按钮维持两列网格；版本按钮在窄卡片内单独占一行，8px 间距与现有节奏一致。
- 颜色与视觉令牌：继续使用现有 `secondary`、`accent`、`line` 和卡片背景令牌。
- 图片与资产：该区域没有产品图片或非标准图标，不涉及替换或降质。
- 文案与内容：`刷新`、`检查更新`、版本号和状态文案均保持原样。

### 最终结论

没有剩余的 P0、P1 或 P2 视觉问题；本轮没有需要阻断交付的 P3 项。

最终结果：通过。
