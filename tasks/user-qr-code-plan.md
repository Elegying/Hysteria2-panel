# v0.19.0 用户节点二维码实施计划

1. 以测试固定二维码矩阵、按需分享响应与初始 HTML 无凭据边界。
2. 引入固定上游版本的纯 Python QR 编码器，增加受限矩阵构建函数。
3. 在现有分享动作上增加 `qr=1` 分支，不改变默认 JSON 契约。
4. 增加每用户二维码按钮、Canvas 弹窗、复制和 PNG 保存；适配桌面与手机。
5. 同步安装器下载/校验/安装、版本和用户文档。
6. 执行全量自动化、静态检查和真实浏览器双视口验收。
7. 对静止快照做安全、代码质量、移动端和发布审查。
8. 提交推送、创建 v0.19.0 发布，并滚动部署三台服务器。
